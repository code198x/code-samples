NEW
10 PRINT "READY"
20 GOSUB 900:END
900 REM TRACE MEMORY
910 PRINT "START OF BASIC:";PEEK(43)+256*PEEK(44)
920 PRINT "START OF VARIABLES:";PEEK(45)+256*PEEK(46)
930 PRINT "END OF STRINGS:";PEEK(51)+256*PEEK(52)
940 L=PEEK(43)+256*PEEK(44)
950 PRINT:PRINT "LINE TABLE"
960 WHILE PEEK(L)<>0
970 NEXTLINE=PEEK(L)+256*PEEK(L+1)
980 LINENUM=PEEK(L+2)*256+PEEK(L+3)
990 PRINT LINENUM;" -> ";NEXTLINE
1000 P=L+4
1010 PRINT "  TOKENS:";
1020 WHILE P< NEXTLINE AND PEEK(P)<>0
1030 T=PEEK(P):PRINT T;
1040 IF T>127 THEN PRINT " (TOKEN)";
1050 P=P+1
1060 WEND
1070 PRINT
1080 L=NEXTLINE
1090 WEND
